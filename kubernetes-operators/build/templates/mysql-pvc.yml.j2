apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ name }}-pvc"
spec:
  storageClassName: {{ storage_class }}
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: {{ storage_size }}
